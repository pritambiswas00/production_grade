import * as React from 'react';

interface HomeProps {}

const Home: React.FC<HomeProps> = () => {
  return (
    <React.Fragment>
      <div className="bg-transparent w-full flex justify-end items-center p-2 relative"></div>
    </React.Fragment>
  );
};

export default Home;

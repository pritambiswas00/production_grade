import * as React from 'react';

interface AddAccountProps {}

export const AddAccount: React.FC<AddAccountProps> = () => {
  return (
    <React.Fragment>
      <div>Add Account</div>
    </React.Fragment>
  );
};
